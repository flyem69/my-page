const config = require('../../../config')
const axios = require('axios')

restClient = {
    getStreams(token) {

    },
    addStream(token) {

    },
    deleteStream(token) {

    }
}

module.exports = restClient